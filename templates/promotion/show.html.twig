{% extends 'base.html.twig' %}

{% block title %}D'étail d'une promotion {% endblock %}

{% block body %}
<style>
  .example-wrapper {margin: 1rem;}
  .date {color: red; font-weight: bold;}
</style>

    <div class="example-wrapper">
      <h1> {{ promotion.nom}}! </h1>
      <p>Description : {{promotion.description }}</p>
      {% if promotion.description |trim is empty %}
      <p>Description n'existe pas</p>
      {% endif %}
      <p>document Pdf : {{promotion.documentPdf }}</p>
      
      <p class="date">Date debut : {{ promotion.debut|date("d/m/Y") }}</p>
      <p class="date">Date de fin : {{ promotion.fin|date("d/m/Y") }}</p>
      <p class="date">Affichage de : {{ promotion.AffichageDe|date("d/m/Y") }}</p>
      <p class="date">Affichage jusque : {{ promotion.AffichageJusque|date("d/m/Y") }}</p>
        
    </div>
{% endblock %}